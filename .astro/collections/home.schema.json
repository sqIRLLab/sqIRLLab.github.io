{
  "$ref": "#/definitions/home",
  "definitions": {
    "home": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "errorMessage": {
            "minLength": "Cannot be empty"
          }
        },
        "subtitle": {
          "$ref": "#/definitions/home/properties/title"
        },
        "description": {
          "$ref": "#/definitions/home/properties/title"
        },
        "email": {
          "type": "string",
          "format": "email",
          "errorMessage": {
            "format": "Must be a valid email address"
          }
        },
        "logo": {
          "type": "string",
          "pattern": "\\.(jpg|jpeg|png|gif|webp|svg)$",
          "errorMessage": {
            "pattern": "Image must be a valid image file (.jpg, .jpeg, .png, .gif, .webp, or .svg)"
          }
        },
        "$schema": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "subtitle",
        "description",
        "email",
        "logo"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}